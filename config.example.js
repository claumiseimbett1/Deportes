/**
 * Copia este archivo como config.js y pon tu Google API Key.
 * config.js está en .gitignore y no se sube al repositorio.
 *
 *   cp config.example.js config.js
 *
 * En Netlify: variable de entorno GOOGLE_API_KEY (se genera config.js al desplegar).
 */
window.APP_CONFIG = {
    API_KEY: 'tu-google-api-key-aqui'
};
